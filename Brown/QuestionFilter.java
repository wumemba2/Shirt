import java.io.Console;

import jdk.jshell.spi.ExecutionControl.StoppedException;
import jdk.nashorn.internal.runtime.StoredScript;

public class QuestionFilter {
	
	public static void main(String[] args) {
		Console console = System.console();

        String[] inputs = {"A.  The field is saved/updated", "B.  When the record is saved/updated", 
                            "C.  When the return key is pressed", "D.  None of the above", "Marlon", "Randy"};
        String storedQuestion [inputs];
        int numberOfPutts = 0;
        boolean inputHasSpace = false;

        
        console.printf("Qustion 1: %n");

        for (String input : inputs) {
            console.printf("%s  %n", input);

        } if (inputHasSpace) {
            
        } else {
            
        } (storedQuestion.contains("A.  ")); {

            console.printf("%s", input); 
        }

                   
        }
    
	}